# Starter Pack Template

Copy this entire folder to start creating your own sound pack:
1. Replace audio files in `audio/` with your own `.ogg` or `.wav` sounds.
2. Edit `pack.json` with your pack name, ID, author, and sound mappings.
3. Validate and package your pack using `python tools/package_pack.py`.
